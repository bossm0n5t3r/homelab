# GoatCounter

Lightweight analytics for the blog.

## Commands

Start:

    docker compose up -d

Stop:

    docker compose down

Logs:

    docker compose logs -f

Update:

    docker compose pull
    docker compose up -d

## Storage

SQLite data is stored in:

    ./data
